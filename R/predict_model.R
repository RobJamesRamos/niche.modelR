# 1) Takes the GLM model and raster stack and creates an output raster
#
# Args
# input_predictors             A raster stack
# input_model                  A GLM model object
#
# Output
# pb                     A raster of predicted liklehood of occurance
predict_model <- function(input_predictors, input_model){
}